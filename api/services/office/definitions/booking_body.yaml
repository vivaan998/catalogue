type: object
required:
  - live_uuid
  - tokens
properties:
  live_uuid:
    type: string
    format: uuid
    description: 'UUID of the live to be booked'
  tokens:
    type: integer
    description: 'Amount of tokens to pay'
    example: 2
    minimum: 0
