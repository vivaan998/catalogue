type: object
required:
  - ref_uuid
allOf:
  - $ref: '#/definitions/AvailabilityBody'
  - properties:
      booked_slots:
        type: integer
        description: 'Number of booked slots'
        default: 0

