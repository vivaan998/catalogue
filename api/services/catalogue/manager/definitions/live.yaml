type: object
#required:
#  - session_uuid
allOf:
  - $ref: '#/definitions/LiveBody'
  - properties:
      uuid:
        type: string
        format: uuid
        description: 'UUID of the live'
      session_uuid:
        type: string
        format: uuid
        description: 'UUID of the session'
